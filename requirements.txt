# NOC – New OpenClaw
# Core (no heavy dependencies; all optional backends are extras)

# Optional – faster HTTP for Ollama / vLLM clients
requests>=2.28.0

# Optional – local LLM backends (install whichever you use)
# llama-cpp-python>=0.2.0       # for LocalLLMBackend.LLAMACPP
# transformers>=4.38.0          # for LocalLLMBackend.TRANSFORMERS
# torch>=2.0.0                  # required by transformers

# Optional – faster tokenisation (drop-in replacement for estimate_tokens)
# tiktoken>=0.6.0

# Optional – proper vector similarity / ANN index
# faiss-cpu>=1.7.4              # Facebook FAISS (CPU build)
# numpy>=1.24.0                 # required by FAISS / most embedding libs

# Development / testing only
pytest>=7.4.0
